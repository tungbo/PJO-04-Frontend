export const GetUserInfo = (state) => state.AuthReducer.UserInfo;

export const GetProductDetail = (state) => state.ProductDetailReducer.product;

export const GetUserDetail = (state) => state.UserDetailReducer.user;

export const GetPizzaDetail = (state) => state.PizzaDetailReducer.pizza;

export const GetSizeDetail = (state) => state.SizeDetailReducer.size;
